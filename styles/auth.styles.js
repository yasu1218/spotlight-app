import { StyleSheet } from "react-native";
/**
 * Styles for the authentication screens.
 * @module styles/auth
 */
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  title: {
    color: "red",
    fontSize: 50,
  }
});
